package com.sdr.metadata.model.impl;

import com.liferay.portal.kernel.exception.SystemException;

import com.sdr.metadata.model.Collection;
import com.sdr.metadata.service.CollectionLocalServiceUtil;

/**
 * The extended model base implementation for the Collection service. Represents a row in the &quot;sdr_Collection&quot; database table, with each column mapped to a property of this class.
 *
 * <p>
 * This class exists only as a container for the default extended model level methods generated by ServiceBuilder. Helper methods and all application logic should be put in {@link CollectionImpl}.
 * </p>
 *
 * @author jaesung
 * @see CollectionImpl
 * @see com.sdr.metadata.model.Collection
 * @generated
 */
public abstract class CollectionBaseImpl extends CollectionModelImpl
    implements Collection {
    /*
     * NOTE FOR DEVELOPERS:
     *
     * Never modify or reference this class directly. All methods that expect a collection model instance should use the {@link Collection} interface instead.
     */
    @Override
    public void persist() throws SystemException {
        if (this.isNew()) {
            CollectionLocalServiceUtil.addCollection(this);
        } else {
            CollectionLocalServiceUtil.updateCollection(this);
        }
    }
}
